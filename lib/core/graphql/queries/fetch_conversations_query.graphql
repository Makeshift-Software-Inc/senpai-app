query fetchConversations($userId: ID!) {
  fetchConversations(userId: $userId) {
    id
    matchId
    messages {
      content
      sender_id
      reaction
      sticker { 
        id
        url
      }
      attachment
    }
  }
}
