query fetchUser($userId: ID!) {
  fetchUser(userId: $userId) {
    id
    lastSignInIp
    matches {
      id
      matcheeId
      conversation {
        id
      }
    }
    gallery {
      photoUrls
    }
  }
}
