mutation addFavoriteMusic($input: AddFavoriteMusicInput!) {
  addFavoriteMusic(input: $input) {
    user {
      id
      favoriteMusic {
        id
        musicType
        trackName
        artistName
        coverUrl
        spotifyId
      }
    }
  }
}
