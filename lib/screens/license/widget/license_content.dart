import 'package:flutter/material.dart';

class LicenseContent extends StatelessWidget {
  const LicenseContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("To be implemented"),
    );
  }
}
